name: Sync main and staging
run-name: Sync staging after PR merge to main

on:
  pull_request:
    types: [closed]
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: sync-staging-after-main-merge
  cancel-in-progress: false

jobs:
  merge-main-into-staging:
    name: Merge main into staging after PR merge
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Checkout staging
        uses: actions/checkout@v4
        with:
          ref: staging
          fetch-depth: 0
          token: ${{ secrets.BYPASS_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Smart sync main to staging
        env:
          GITHUB_TOKEN: ${{ secrets.BYPASS_TOKEN || secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail

          echo "🔍 Verifying token is available..."
          if [ -z "$GITHUB_TOKEN" ]; then
            echo "❌ No token available!"
            exit 1
          fi
          echo "✅ Token is configured"
          echo ""

          echo "🔍 Fetching latest main and staging branches..."
          git fetch origin main:origin-main staging:origin-staging
          echo ""

          echo "🔧 Configure git for sync commit..."
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          echo ""

          echo "🔄 Ensuring we're on latest staging..."
          git checkout staging
          git reset --hard origin-staging
          echo ""

          # Create empty sync commit to mark the sync point
          echo "🔀 Creating empty sync commit..."
          echo "ℹ️  This empty commit marks the sync point without merge history"

          # Get the current main SHA
          MAIN_SHA=$(git rev-parse origin-main)
          echo "📋 Main SHA: ${MAIN_SHA:0:7}"

          # Create empty commit with main SHA in message
          STAGING_SHA=$(git rev-parse HEAD)
          git commit --allow-empty -m "Sync: main@${MAIN_SHA:0:7} after PR #${PR_NUMBER}"
          echo "✅ Empty sync commit created"

          echo "⬆️ Pushing staging to remote..."
          # The token from actions/checkout should already be configured, but let's be explicit
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git staging
          echo "✅ Main successfully synced to staging!"

  delete-production-promoter-branch:
    name: Delete production promoter branch after PR merge to main
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true && github.event.pull_request.head.ref == 'production-promoter'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete production promoter branch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          echo "🧹 Deleting remote branch production-promoter if present..."
          if git ls-remote --heads origin production-promoter | grep -q 'refs/heads/production-promoter'; then
            git push origin --delete production-promoter || true
            echo "✅ Deleted remote branch production-promoter"
          else
            echo "ℹ️ Remote branch production-promoter not found; skipping"
          fi
